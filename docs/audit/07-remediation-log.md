# 07 — Remediation Log

Live log of every material change made during the production-hardening audit.
Branch: `audit/production-hardening`. Baseline: commit `e3cf076` + uncommitted Phase 1–6 working tree.

| Change ID | Finding Addressed | Files Changed | Implementation Summary | Tests Added or Updated | Validation Result | Residual Risk |
|-----------|-------------------|---------------|------------------------|------------------------|-------------------|---------------|
| R-001 | `migrations/0001_baseline.sql` not deployable via `prisma migrate deploy` (flat file, no directory layout, no lock file) | `packages/db/prisma/migrations/20260801000000_baseline/migration.sql`, `migration_lock.toml` | Restructured baseline into Prisma's expected `<ts>_<name>/migration.sql` layout; removed the flat file | none (layout change) | `prisma validate` passes; `migrate deploy` on a live DB still unproven — Docker unavailable | Migration deploy on real Postgres unverified (Stage G blocker) |
| R-002 | `ThrottlerGuard` applied twice on auth controller (controller-level `@UseGuards` on top of global guard) — double-counted rate limits | `apps/api/src/auth/auth.controller.ts` | Removed controller-level `@UseGuards(ThrottlerGuard)`; global guard remains the single enforcement point | none | API test suite green | none |
| R-003 | `*.tsbuildinfo` build artifacts polluting git status | `.gitignore` | Added `*.tsbuildinfo` | none | `git status` clean of build artifacts | none |
| R-004 | `FAILED` webhook events permanently blocked reprocessing; non-financial `ADJUSTMENT` events wrote zero-value ledger rows | `apps/api/src/billing/billing-webhook.service.ts` | `FAILED` `ProcessorEvent` rows are deleted on re-ingest so processing retries; zero-value `Transaction`/`LedgerEntry` rows only created for financial event types | `billing-webhook.service.spec.ts` (replay, failure-reprocess, zero-value cases) | Suite green | Processor-specific semantics still stub-based |
| R-005 | Admin actions (moderation resolve, payout approve/paid/cancel, hold release, geo-block create/remove, DMCA status) wrote no audit trail | `apps/api/src/admin/admin.service.ts`, `admin.controller.ts`, `admin.module.ts`, new `dto/create-geo-block.dto.ts`, `dto/update-dmca-status.dto.ts` | Every admin mutation now records `adminUserId` + action payload via a shared `audit()` helper; controller passes `CurrentUser` through | existing admin specs updated | Suite green | Audit log export format not yet contract-tested |
| R-006 | `GeoBlock` had no scope; payout geography rules unenforced; creators had no `countryCode` | `packages/db/prisma/schema.prisma`, new migration `20260801010000_email_tokens_geo_scope/migration.sql`, `apps/api/src/risk/jurisdiction.service.ts`, `risk.module.ts`, `apps/api/src/payouts/payouts.service.ts`, `apps/api/src/admin/admin.service.ts` | Added `GeoBlockScope` (ACCESS/PAYOUTS) + `GeoBlock.scope`, `CreatorProfile.countryCode`; `JurisdictionService.payoutDecisionForCreator` gates payout request AND admin payout approval | `jurisdiction.service.spec.ts`, `payouts.service.spec.ts` updated | Suite green | Migration not yet applied to a live DB |
| R-007 | TOTP enrolled users could log in without a TOTP code | `apps/api/src/auth/auth.service.ts`, `auth/dto/login.dto.ts` | `login()` verifies TOTP when enabled; `totp_required` / `invalid_totp` distinct errors; TOTP checked before account-status checks | `auth.service.spec.ts` TOTP cases | Suite green | TOTP replay window is otplib default (documented) |
| R-008 | No email verification or password reset flows; email provider interface existed with no implementation | new `apps/api/src/mailer/mailer.{service,module}.ts`, `auth.service.ts`, `auth.controller.ts`, `auth.module.ts`, new DTOs (`verify-email.dto.ts`, `email-address.dto.ts`, `password-reset-confirm.dto.ts`), `register.dto.ts` (72-char cap) | `MailerService` implements shared `EmailProvider` (log-only default, `EMAIL_PROVIDER`/`EMAIL_FROM` config); registration creates `EmailToken` + verification email and withholds tokens when `AUTH_AUTO_ACTIVATE=false`; verify/resend/reset-request/reset-confirm endpoints; enumeration-safe responses; sessions revoked on password reset | `auth.service.spec.ts` full coverage incl. token expiry, race on consume, enumeration | Suite green (86 tests) | **No real email provider adapter** — production email delivery blocked until SES/Postmark/Resend adapter is written |
| R-009 | Creators could not set `countryCode` (needed for payout jurisdiction) | `apps/api/src/creator/creator.service.ts`, `creator.controller.ts`, new `dto/update-profile.dto.ts` | `PATCH /v1/creator/profile` updates displayName/bio/countryCode with audit log on countryCode change | existing creator specs | Suite green | none |
| R-010 | **Phantom code in worker entry**: `main.ts` imported non-existent `./media-pipeline` and non-existent shared exports (`CloudflareStreamAdapter`, `createModerationAdapter`) plus phantom env vars (`MEDIA_WEBHOOK_SECRET`, `MEDIA_TOKEN_SECRET`) — worker could not compile or run | `apps/worker/src/main.ts` | Rewrote `main.ts` to compose the modules that actually exist: `R2Storage`/`r2StorageConfigFromEnv`, `CSAM_PROVIDER` factory (noop/hashlist), `promoteIfClean`, `JobFailureAlerter`; retryable CSAM scan errors now throw so BullMQ `attempts:5` exponential backoff engages instead of quarantining unchecked media; blocked/suspect media stays quarantined in staging (evidence preservation, no auto-delete) | none (worker has no test harness — flagged in gap register) | `tsc --noEmit -p tsconfig.json` passes | Worker pipeline still untested at job level |
| R-011 | Worker had no graceful shutdown — SIGTERM dropped in-flight jobs mid-write | `apps/worker/src/main.ts` | `SIGINT`/`SIGTERM` → `worker.close()` (drains) → `prisma.$disconnect()` → Redis disconnect, with 30s force-exit guard and idempotency flag | none | Compiles; behavior to be smoke-tested in Stage G | none |
| R-012 | Worker Stream config used `CF_STREAM_ACCOUNT_ID`/`CF_STREAM_API_TOKEN` while `promote.ts` and `.env.example` used `CLOUDFLARE_ACCOUNT_ID`/`CLOUDFLARE_STREAM_API_TOKEN` — Stream ingest silently never configured | `apps/worker/src/main.ts` | Standardized on `CLOUDFLARE_*` names | none | Compiles | none |
| R-013 | API had no security headers, no CORS policy, no proxy trust setting, no shutdown hooks, no startup config validation | `apps/api/src/main.ts`, new `apps/api/src/config/env.validation.ts`, `helmet` dependency | helmet (CSP off — API serves JSON only); CORS allowlist from `CORS_ORIGINS` (deny-all default; required in production); `trust proxy 1`; `enableShutdownHooks()`; `validateEnv(config)` before listen | `env.validation.spec.ts` (9 tests: required vars, DB URL scheme, prod extras, weak-secret rejection, CORS requirement, warning paths) | Suite green; full workspace `pnpm typecheck` green | helmet CSP for web/admin frontends still frontend-owned |
| R-014 | `validateEnv` tests flaky: `ConfigService.get()` prioritizes `process.env` over constructor config, so ambient shell env (`DATABASE_URL`, Jest's `NODE_ENV=test`) leaked into unit tests | `apps/api/src/config/env.validation.ts`, `env.validation.spec.ts` | Introduced `EnvReader` interface (`{ get(key): string \| undefined }`); runtime uses `ConfigService`, tests use isolated object-backed reader | spec rewritten on `EnvReader` | 9/9 pass deterministically | none |
| R-015 | Duplicate `ThrottlerModule.forRoot` in `AuthModule` (AppModule already registers globally) | `apps/api/src/auth/auth.module.ts` | Removed module-level registration | none | Suite green | none |
| R-016 | GeoBlockGuard with **no** `GEO_EDGE_SHARED_SECRET` in production trusted the spoofable `cf-ipcountry` header (false compliance assurance) | `apps/api/src/admin/geo-block.guard.ts`, `geo-block.guard.spec.ts` | Production + no secret → header treated as untrusted, enforcement honestly disabled (with loud startup warning from `validateEnv`); non-production keeps trusting header for local testing | 2 new guard tests (prod-no-secret fail-open, dev-no-secret enforces) | 12/12 guard tests pass | Operators must set `GEO_EDGE_SHARED_SECRET` at the edge for ACCESS geo-blocking to be real |
| R-017 | `.env.example` missing variables for features added during audit; admin port wrong | `.env.example` | Added `PORT`, `CORS_ORIGINS`, `APP_WEB_URL`, `EMAIL_PROVIDER`, `EMAIL_FROM`, `GEO_EDGE_SHARED_SECRET` with comments on production requirements; corrected admin origin to `:3010` | none | `validateEnv` spec covers the required set | none |
| R-018 | No `typecheck` script anywhere; type errors only surfaced ad hoc | root `package.json`, `turbo.json`, all 6 package `package.json` files | `typecheck` script per package (api runs build+spec configs; db runs `prisma validate`); `turbo typecheck` root task with `^build` ordering and `DATABASE_URL` env passthrough (turbo strict env) | none | **7/7 turbo typecheck tasks pass** | none |
| R-019 | `NoopCsamScanProvider` duplicated verbatim in `apps/worker/src/noop-csam.provider.ts` and `apps/api/src/integrations/noop-csam.provider.ts` | `packages/shared/src/adapters/csam-scan.ts`, deleted both app-level copies, updated `apps/worker/src/main.ts` import | Single authoritative `NoopCsamScanProvider` in `@hushd/shared`; worker imports from shared | none | `pnpm typecheck` 7/7 green; API suite 88/88 green | none |
| R-020 | Dead DI tokens: `CSAM_PROVIDER` and `EMAIL_PROVIDER` were provided/exported by `IntegrationsModule` but had **zero** `@Inject()` consumers (API CSAM scanning is worker-owned; email goes through `MailerModule`) | `apps/api/src/integrations/integrations.tokens.ts`, `integrations.module.ts`, deleted `noop-email.provider.ts` | Removed unused providers, exports, token constants, and the orphaned `NoopEmailProvider` | none | `pnpm typecheck` green; API suite green | none |
| R-021 | Worker media pipeline had no unit tests (G-010) — the safety-critical scan-and-ingest gate lived inline in `main.ts` BullMQ wiring, untestable without a live queue/Redis/storage | new `apps/worker/src/media-pipeline.ts` (extracted `processScanAndIngest`), `apps/worker/src/media-pipeline.spec.ts`, `apps/worker/src/main.ts`, `apps/worker/package.json`, `apps/worker/tsconfig.json`, new `apps/worker/tsconfig.spec.json`, new `apps/worker/jest.config.cjs` | Extracted the CSAM-gate decision tree into a pure, dependency-injected function; worker now has a Jest harness | `media-pipeline.spec.ts` (5 cases: clean→promote, match→block+quarantine, suspect→quarantine+escalate, retryable error→throw for backoff, non-retryable error→quarantine+escalate) | Worker suite 5/5 green; `tsc` build + spec configs pass | none |
| R-022 | No request correlation IDs (G-018) — a single request could not be traced across API logs, error responses, and Sentry reports | new `apps/api/src/observability/request-id.middleware.ts`, `apps/api/src/main.ts`, `apps/api/src/observability/http-exception.filter.ts` | Middleware propagates a safe inbound `x-request-id` (else mints a UUID), attaches it to the request, echoes it on the response header; the exception filter surfaces it in logs, Sentry context, and the JSON error body | `request-id.middleware.spec.ts` (5 cases: propagate, mint, reject unsafe chars, reject over-length, array header) | Suite green (10 new tests) | none |
| R-023 | Rate limiting used default in-memory storage (G-012) — per-process limits silently multiplied by replica count in production, defeating distributed throttling | new `apps/api/src/observability/redis-throttler.storage.ts`, `apps/api/src/app.module.ts` | Redis-backed `ThrottlerStorage` with an atomic Lua-script increment; sliding-window counter + absolute block window; fails closed (throws → 503) on Redis failure rather than silently dropping rate limiting | `redis-throttler.storage.spec.ts` (5 cases: record mapping, blocked flag, key scoping + key expiry, failure propagation, disconnect) | Suite green (10 new tests) | Lua sliding-window semantics not covered by a live Redis integration test (unit-mocked) |

## Verification commands run (this batch)

| Check | Command | Result |
|-------|---------|--------|
| API typecheck (build) | `pnpm --filter @hushd/api exec tsc --noEmit -p tsconfig.build.json` | PASS |
| API typecheck (spec) | `pnpm --filter @hushd/api exec tsc --noEmit -p tsconfig.spec.json` | PASS |
| Worker typecheck | `pnpm --filter @hushd/worker exec tsc --noEmit -p tsconfig.json` | PASS |
| API unit tests | `pnpm --filter @hushd/api test` | PASS — 13 suites, 88 tests |
| Workspace typecheck | `pnpm typecheck` | PASS — 7/7 turbo tasks |
| DB schema validation | part of `turbo typecheck` (`prisma validate`) | PASS |
| Migration deploy on live Postgres | `prisma migrate deploy` | **NOT RUN — Docker/Desktop unavailable** (Stage G blocker) |

---

## Continuation batch (2026-08-01 afternoon)

| Change ID | Finding Addressed | Files Changed | Implementation Summary | Tests Added or Updated | Validation Result | Residual Risk |
|-----------|-------------------|---------------|------------------------|------------------------|-------------------|---------------|
| R-024 | Media MIME/size optional → bypassable upload validation | `media.service.ts`, `dto/init-upload.dto.ts`, `media.controller.ts`, `media.service.spec.ts` | `contentType` + `byteSize` required; DTO validated; empty content type rejected | missing content type case | API 104 tests green | Client must send accurate declared size (R2 still accepts bytes within signed URL) |
| R-025 | Windows Next standalone `EPERM` symlink build failure | `apps/web/next.config.ts`, `apps/admin/next.config.ts`, Dockerfiles | Skip standalone on win32 unless `NEXT_STANDALONE=1`; Docker sets it; always set `outputFileTracingRoot` | none | `pnpm turbo build` web+admin+api+worker PASS on Windows | Operators on Windows must not force standalone without Developer Mode |
| R-026 | Prisma baseline migration UTF-16; deploy unproven | `migrations/20260801000000_baseline/migration.sql` | Re-encoded UTF-8; clean `migrate deploy` of both migrations | none | **PASS** — schema up to date on localhost:5432 | Host vs compose port clash documented (compose→5433) |
| R-027 | Compose no healthchecks; 5432 clash | `docker-compose.yml`, `.env.example` | Healthchecks; host port **5433** | none | File updated | Update local `.env` if using compose |
| R-028 | Creator subscribe empty `tierId`; payout SWIFT; admin role restore; billing cancel | feed profile API, creator page, payouts page, admin require-auth, billing list/cancel | Functional UX/API repairs from audit | feed.service.spec profile cases | Suite green | Discovery/search still thin |
| R-029 | CI weak | `.github/workflows/ci.yml` | Postgres migrate + typecheck + test + docker matrix + audit | none | Config present; **remote CI not yet proven** | Push branch to verify Actions |
| R-030 | Next high CVEs; multer/lodash | `apps/*/package.json`, root `pnpm.overrides` | Next ^15.5.18; override multer≥2.1.1, lodash≥4.18.0 | none | Install + build PASS | Re-run `pnpm audit:deps` periodically |
| R-031 | Prod could boot with unsafe stubs | `env.validation.ts` (+spec), `mailer.service.ts`, `worker/main.ts`, `.env.example` | Prod fails closed on log email, auto-activate, noop IDV, stub processors; Resend adapter; worker rejects noop CSAM in prod | 3 new env tests | API 104 green | Live processors still not implemented — prod boot requires real ids |

## Verification commands (continuation)

| Check | Command | Result |
|-------|---------|--------|
| Typecheck | `pnpm typecheck` | PASS |
| API tests | `pnpm --filter @hushd/api test` | PASS — 104 tests |
| Worker tests | (turbo test) | PASS — 6 tests |
| Production build | `pnpm turbo build --filter=@hushd/{web,admin,api,worker}` | PASS |
| Migrate deploy | `npx prisma migrate deploy` (DATABASE_URL local) | PASS — 2 migrations |
| Docker API image | `docker build -f apps/api/Dockerfile` (earlier) | PASS |
| Remote GitHub Actions | push + watch | **NOT RUN** |

---

## Phase 7 batch (2026-08-01 evening) — cookie auth + OpenAPI sync + e2e scaffold

| Change ID | Finding Addressed | Files Changed | Implementation Summary | Tests Added or Updated | Validation Result | Residual Risk |
|-----------|-------------------|---------------|------------------------|------------------------|-------------------|---------------|
| R-032 | G-005 — JWT access+refresh tokens in `localStorage` (XSS session exfiltration) | new `apps/api/src/auth/cookies.ts`, `apps/api/src/auth/csrf.middleware.ts`; `auth.controller.ts`, `jwt-auth.guard.ts`, `main.ts`, `dto/refresh.dto.ts`, `config/env.validation.ts`; `apps/web/lib/api.ts`, `apps/web/lib/auth-context.tsx`, `apps/web/app/verify-email/page.tsx`; `apps/admin/lib/api.ts`, `apps/admin/lib/auth-context.tsx`, `apps/admin/lib/require-auth.tsx`; `.env.example`; new `docs/adr/0006-httponly-cookie-auth.md` | Browser auth moved to HttpOnly `Secure` `SameSite` cookies (`hushd_access` / `hushd_refresh`, refresh scoped to `/v1/auth`). `JwtAuthGuard` accepts Bearer **or** access cookie. New `csrfHeaderMiddleware` requires `X-Requested-With: XMLHttpRequest` on cookie-authenticated mutating requests. FE API clients send `credentials:"include"` + the CSRF header and no longer touch `localStorage`; refresh posts with empty body (cookie). `validateEnv` enforces `SameSite=None` ⇒ `Secure`. Bearer-token API clients unaffected | `cookies.spec.ts`, `csrf.middleware.spec.ts`, `jwt-auth.guard.spec.ts` cookie cases, `env.validation.spec.ts` cookie rules | API 132/132 green; workspace typecheck 8/8; prod build 6/6 | `COOKIE_SECURE=false` only viable on localhost; cross-site SPA deployments must set `SameSite=None`+`Secure` |
| R-033 | G-008 — OpenAPI drift | `docs/openapi.yaml` | Full re-sync to implemented controllers: auth (verify/resend/reset/TOTP), media, billing subscriptions, creator profile/tiers/dashboard, all admin/payouts/moderation/verification/analytics routes, `/health/ready`. Added `cookieAuth` security scheme; corrected enums (`PayoutMethod`, `ReportTargetType`, `DMCAStatus`, `GeoBlockScope`) to Prisma; fixed regex `pattern` escaping | none (doc) | `yaml.parse` clean; version bumped 0.7.0 | ~~Spec is hand-maintained~~ — automated contract guard added (R-037) |
| R-034 | G-010 — no e2e harness | new `e2e/` workspace (`package.json`, `playwright.config.ts`, `tests/health.api.spec.ts`, `tests/auth.api.spec.ts`, `tests/web.ui.spec.ts`); `pnpm-workspace.yaml`; root `package.json` `test:e2e` | Playwright scaffold with two projects: `api` (baseURL API, sends CSRF header) covering health + full cookie session lifecycle (register→me→cookie refresh→CSRF 403→logout), and `chromium` covering web home/login render + register flow asserting **no** `localStorage` tokens | 7 e2e specs | `playwright test --list` discovers 7/7; `@hushd/e2e` typechecks in turbo | Browsers not yet installed in this env (`playwright install --with-deps chromium`); UI specs need running stack |

## Verification commands (Phase 7)

| Check | Command | Result |
|-------|---------|--------|
| Install (incl. e2e workspace) | `pnpm install` | PASS — 8 workspace projects |
| Workspace typecheck | `pnpm typecheck` | PASS — 8/8 turbo tasks (now includes `@hushd/e2e`) |
| API unit tests | `pnpm --filter @hushd/api test` | PASS — 18 suites, 132 tests |
| Worker unit tests | `pnpm --filter @hushd/worker test` | PASS — 6 tests |
| Production build | `pnpm turbo build --filter=@hushd/{web,admin,api,worker}` | PASS — 6/6 |
| E2E discovery | `pnpm --filter @hushd/e2e exec playwright test --list` | PASS — 7 tests in 3 files |
| E2E live run | `playwright test` (needs stack + browsers) | **NOT RUN — no running stack/browsers in this env** |

---

## Residual-notes batch (2026-08-01 night) — markdown audit, table repair, OpenAPI contract guard

| Change ID | Finding Addressed | Files Changed | Implementation Summary | Tests Added or Updated | Validation Result | Residual Risk |
|-----------|-------------------|---------------|------------------------|------------------------|-------------------|---------------|
| R-035 | Markdown lint decision — audit found **no** markdown lint gate configured anywhere: package scripts, CI workflows (`ci.yml`, `deploy.yml`, `security.yml`), pre-commit hooks, editor config, Makefiles, task runners, repo scripts (searched for `markdownlint`/`remark`/`mdlint` — zero hits) | none (decision recorded) | Per the no-unconfigured-rewrite rule, no cosmetic restyling was performed. Classification: compact table pipe spacing and emphasis-as-heading patterns are **harmless stylistic conventions**, consistent within each document, and parse correctly as GFM. The only **genuine defect** found was the malformed R-019/R-020 table rows (8 parsed cells vs 7-column header) — repaired as R-036. No ambiguous headings, broken renders, or tooling-blocking structures found in `docs/audit/*.md` | none | Structural scan of all remediation rows (escape-aware pipe counting) — only R-019/R-020 malformed | If the team later adopts `markdownlint-cli2`, expect style-only warnings; configure rather than rewrite |
| R-036 | R-019/R-020 rows in this log had a doubled leading pipe (`\|\| R-019 …`) → 8 parsed cells instead of 7 | `docs/audit/07-remediation-log.md` (2 lines) | Repaired with a targeted one-shot Node script: located rows by ID regex (`^\|\| (R-019\|R-020) \|`), removed exactly one leading delimiter, verified header = 7 columns, verified both rows 8→7 cells, verified **all** R-xxx rows have 7 cells with escape-aware counting (`\|` literals ignored — R-014's `string \| undefined` is correct GFM), preserved CRLF/EOL and all cell text byte-for-byte; script deleted after use | none (doc structure) | Script self-checks passed; `git diff` shows exactly 2 changed lines, cell contents untouched | none |
| R-037 | R-033 residual — hand-maintained OpenAPI spec could silently drift from implemented controllers | new `apps/api/src/openapi/` (`contract-types.ts`, `controller-routes.ts`, `openapi-document.ts`, `contract-diff.ts`, `openapi-contract.spec.ts`); `apps/api/package.json` (`test:contract`, devDep `yaml`); `.github/workflows/ci.yml` (dedicated CI step); drift fixes in 9 controllers + `docs/openapi.yaml` | Contract guard extracts every NestJS route via `reflect-metadata` (no app boot — deterministic, no DB/env/credentials needed), normalizes `:param`→`{param}`, strips the `v1` global prefix (read from `main.ts`), parses `docs/openapi.yaml` (incl. `$ref` component parameters), and diffs: undocumented routes, missing implementations, method mismatch, path-param rename/undeclared, security vs `@Public()`, `@Body` vs `requestBody`, success status vs `@HttpCode`. First run found **41 drift items**, all fixed: 28 handlers pinned to documented `200` via `@HttpCode(200)` (also fixes a latent e2e expectation on `/auth/refresh`); `security` added to `/feed/creator/{slug}`, `/feed/creator/{slug}/profile`, `/moderation/dmca` (spec claimed "public"; runtime + web `RequireAuth` prove auth-required — summaries corrected); `requestBody` added to payout paid/cancel + report triage; parser taught to resolve `#/components/parameters/*` refs (7 false positives) | `openapi-contract.spec.ts` — 16 tests: extraction pins, spec sanity, server-prefix check, zero-drift assertion, 9 negative fixtures proving each mismatch kind fails | **16/16 pass, zero drift**; runs in CI as `OpenAPI contract guard` step before `turbo test` (also inside it via `jest`) | Deep request/response **schema** body comparison not implemented (controllers don't expose DTO schemas at runtime without boot); route/method/param/security/status/body-presence contract is enforced. DMCA intake made public is a product decision — spec now matches current auth-required runtime |

## Verification commands (residual-notes batch)

| Check | Command | Result |
|-------|---------|--------|
| OpenAPI contract guard | `pnpm --filter @hushd/api run test:contract` | PASS — 16/16 tests, zero drift |
| API unit tests (full) | `pnpm --filter @hushd/api test` | PASS |
| Workspace typecheck | `pnpm typecheck` | PASS |
| Lint | `pnpm turbo lint` | PASS |
| Production build | `pnpm turbo build` | PASS |
| Remediation-log table validation | one-shot Node checker (escape-aware cell counts, all R-xxx rows) | PASS — all rows 7 cells |
| CI config validation | `ci.yml` parsed as YAML; contract step runs `test:contract` | PASS (remote run pending push) |
